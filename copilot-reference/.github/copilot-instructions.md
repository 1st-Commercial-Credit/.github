# Copilot Instructions (Repository Standard)

- Follow existing patterns; avoid wide refactors unless requested.
- Prefer small, testable commits.
- Never add secrets; use environment variables and documented secret names.
- Add/adjust tests with every behavior change.
- Update docs when interfaces change.
